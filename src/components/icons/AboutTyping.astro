<svg
  {...Astro.props}
  viewBox="0 0 64 64"
  xmlns="http://www.w3.org/2000/svg"
  fill="none"
  aria-hidden="true"
>
  <rect x="8" y="40" width="48" height="6" rx="2.5" fill="currentColor" opacity="0.2"></rect>
  <rect x="38" y="20" width="14" height="11" rx="2" stroke="currentColor" stroke-width="2"></rect>
  <rect x="40" y="22.5" width="10" height="6" rx="1" fill="currentColor" opacity="0.3"></rect>
  <path d="M42 33h6" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path>

  <circle cx="22" cy="22" r="5" fill="currentColor" opacity="0.85"></circle>
  <path d="M16 33.5c0-4 3-7 6.5-7s6.5 3 6.5 7v2.5H16z" fill="currentColor" opacity="0.55"></path>

  <g class="arm arm-left">
    <path d="M23 35l9 3" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"></path>
  </g>
  <g class="arm arm-right">
    <path d="M28 34l9 2.8" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"></path>
  </g>

  <circle class="key key-1" cx="44" cy="17" r="1.2" fill="#fde68a"></circle>
  <circle class="key key-2" cx="47.2" cy="15.8" r="1.2" fill="#bfdbfe"></circle>
  <circle class="key key-3" cx="50.4" cy="17" r="1.2" fill="#fbcfe8"></circle>
</svg>

<style>
  .arm-left {
    transform-origin: 23px 35px;
    animation: type-left 1s ease-in-out infinite;
  }

  .arm-right {
    transform-origin: 28px 34px;
    animation: type-right 1s ease-in-out infinite;
    animation-delay: 0.12s;
  }

  .key {
    opacity: 0;
    animation: key-pop 1.2s ease-in-out infinite;
  }

  .key-2 {
    animation-delay: 0.18s;
  }

  .key-3 {
    animation-delay: 0.36s;
  }

  @keyframes type-left {
    0%,
    100% {
      transform: rotate(0deg);
    }
    40% {
      transform: rotate(-7deg);
    }
  }

  @keyframes type-right {
    0%,
    100% {
      transform: rotate(0deg);
    }
    40% {
      transform: rotate(6deg);
    }
  }

  @keyframes key-pop {
    0%,
    70%,
    100% {
      opacity: 0;
      transform: translateY(0);
    }
    30% {
      opacity: 1;
      transform: translateY(-1px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .arm-left,
    .arm-right,
    .key {
      animation: none !important;
    }
  }
</style>
