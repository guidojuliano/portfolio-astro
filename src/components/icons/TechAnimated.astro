<svg
  {...Astro.props}
  viewBox="0 0 64 64"
  xmlns="http://www.w3.org/2000/svg"
  fill="none"
  aria-hidden="true"
>
  <rect x="10" y="10" width="44" height="44" rx="12" stroke="currentColor" stroke-width="2" opacity="0.35"></rect>
  <path class="chip" d="M24 24h16v16H24z" fill="currentColor" opacity="0.22"></path>
  <path d="M32 18v6M32 40v6M18 32h6M40 32h6" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path>
  <path class="pulse-ring" d="M32 20a12 12 0 1 1 0 24a12 12 0 0 1 0-24z" stroke="#93c5fd" stroke-width="1.8"></path>
  <circle class="node node-1" cx="22" cy="22" r="2" fill="#f9a8d4"></circle>
  <circle class="node node-2" cx="42" cy="22" r="2" fill="#fde68a"></circle>
  <circle class="node node-3" cx="22" cy="42" r="2" fill="#86efac"></circle>
  <circle class="node node-4" cx="42" cy="42" r="2" fill="#93c5fd"></circle>
</svg>

<style>
  .chip {
    transform-origin: center;
    animation: tech-chip 2s ease-in-out infinite;
  }

  .pulse-ring {
    transform-origin: center;
    animation: tech-ring 1.8s ease-in-out infinite;
  }

  .node {
    animation: tech-node 1.8s ease-in-out infinite;
  }

  .node-2 { animation-delay: 0.15s; }
  .node-3 { animation-delay: 0.3s; }
  .node-4 { animation-delay: 0.45s; }

  @keyframes tech-chip {
    0%, 100% { transform: scale(1); opacity: .22; }
    50% { transform: scale(1.05); opacity: .35; }
  }

  @keyframes tech-ring {
    0%, 100% { opacity: .2; transform: scale(1); }
    50% { opacity: .75; transform: scale(1.08); }
  }

  @keyframes tech-node {
    0%, 100% { transform: scale(1); opacity: .8; }
    50% { transform: scale(1.25); opacity: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    .chip, .pulse-ring, .node { animation: none !important; }
  }
</style>
